# Frontend

- Preserve the project’s design system if one exists.
- Prefer clear information hierarchy over decorative complexity.
- Ensure accessibility and responsive behavior from the start.
- Avoid generic, interchangeable UI patterns when creating new surfaces.
- Keep state, data loading, and rendering boundaries explicit.
