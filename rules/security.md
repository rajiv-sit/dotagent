# Security

- Validate all external input at boundaries.
- Avoid embedding secrets in code or sample configuration.
- Treat file paths, shell input, and network input as untrusted.
- Prefer least-privilege defaults in integrations and credentials.
- Document security-sensitive assumptions in design docs.
